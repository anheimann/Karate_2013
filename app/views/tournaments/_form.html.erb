<%= simple_form_for @tournament, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Tournament</legend>
		
		<%= f.input :name %>
		<%= f.input :date, :start_year => Date.today.year + 5, :end_year => Date.today.year - 75, :order => [:month, :day, :year], :include_blank => true %>
		<%= f.input :min_rank, :collection => Student::RANKS, prompt: "Select..." %>
		<%= f.input :max_rank, :collection => Student::RANKS, prompt: "Select..." %>
		<%= f.input :active %>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', events_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>